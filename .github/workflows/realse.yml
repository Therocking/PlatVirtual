name: Publish Docker

on:
  push:
    branches: [ main ]

env:
  IMAGE_NAME: therocking/plataforma-virtual

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v27

      - name: Enable flakes
        run: |
          mkdir -p ~/.config/nix
          echo "experimental-features = nix-command flakes" >> ~/.config/nix/nix.conf

      - name: Build image
        run: |
          nix build .#docker-amd64

      - name: Load docker image
        run: docker load < result

      - name: Tag image
        run: |
          docker tag plataforma-virtual:latest ghcr.io/${{ env.IMAGE_NAME }}:latest
          docker tag plataforma-virtual:latest ghcr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Login GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          docker push ghcr.io/${{ env.IMAGE_NAME }}:latest
          docker push ghcr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
